## 1. 審查驗證

- [x] 1.1 實作並驗證音訊資料非持久化：確認完整辨識週期中無音訊寫入磁碟 — 驗證：音訊資料非持久化需求
- [x] 1.2 透過 keyring 套件實作系統 keyring API 金鑰加密 — 驗證：透過系統 keyring 加密 API 金鑰需求
- [x] 1.3 在啟動時驗證設定目錄權限（0o700） — 驗證：設定目錄權限需求
- [x] 1.4 透過自訂格式化器實作日誌清理：從日誌中遮蔽使用者文字的 filter — 驗證：透過自訂格式化器進行日誌清理需求

## 2. 相依套件

- [x] 2.1 將 `keyring` 新增至 `pyproject.toml` 相依套件

## 3. 自動化測試套件

- [x] 3.1 建立 `tests/test_security.py` 實作自動化測試套件：音訊非持久化、keyring 使用、設定權限、日誌清理 — 驗證：以自動化測試套件進行審查需求
